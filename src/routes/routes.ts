/**
 * @module routes
 */

export const routes = {
  login: '/login',
}
/* export const protectedRoutes = [
  routes.simulators
]
export const publicRoutes = []
export const authRoutes = [
  routes.login,
  routes.register
]

export const apiAuthPrefix = '/api/auth'
export const DEFAULT_LOGIN_REDIRECT = '/dashboard/simulators' */